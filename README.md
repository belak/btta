# btta

Arcade leaderboard display for Back to the Arcade.

## Usage

```
# Create an admin user
btta users create <username>

# Start the server
btta serve --db btta.db --media-dir media
```

The admin UI is available at `/admin/`.
